<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Serviço fica em foreground para sobreviver à tela apagada -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Necessário em Android 13+ para exibir a notificação do foreground service -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Diagnóstico: listar pacotes/serviços do RobotSDK instalados (descobrir nome real). -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Visibilidade de pacotes (Android 11+/targetSdk 34): permite resolver e bindar
         o serviço do RobotSDK (CSJBot) por ação/componente, mesmo estando em outro app.
         Pacote do app do RobotSDK: com.csjbot.robotsdk.ten. -->
    <queries>
        <package android:name="com.csjbot.robotsdk.ten" />
        <intent>
            <action android:name="com.csjbot.robotsdkservice.startservice" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.KenMotionBridge">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <service
            android:name=".service.BridgeService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Canal de callbacks AIDL: o RobotSdkService faz bind DE VOLTA neste serviço,
             usando o packageName passado em connectToSDK(). Precisa ser EXPORTADO. -->
        <service
            android:name="com.aidlagent.AIDLClientService"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
